/// the class that implements this interface must have a count variable initialised
///

package PartOne.Utility;

import java.util.HashMap;

public interface Array {

    double[] GenArray(int count);
}
